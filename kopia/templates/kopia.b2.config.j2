{
  "storage": {
    "type": "{{ item.type }}",
    "config": {
      "bucket": "{{ item.bucket }}",
      "keyID": "{{ item.accesskey }}",
      "key": "{{ item.secretkey }}"
    }
  },
  "caching": {
    "cacheDirectory": "{{item.cache}}",
    "maxCacheSize": 5242880000,
    "maxMetadataCacheSize": 5242880000,
    "maxListCacheDuration": 30
  },
  "hostname": "{{ this_host }}",
  "username": "{{ kopia_user }}",
  "description": "{{ item.description }}",
  "enableActions": false,
  "formatBlobCacheDuration": 900000000000
}